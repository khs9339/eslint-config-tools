module.exports = {
    // 확장할 설정
    "extends": [ "eslint:recommended" ],
    // 플러그인 추가
    "plugins": [ "import" ],
    // 사용할 규칙 설정
    "rules": {
        "semi": "error",
        "no-console": "error",
    },
  }